{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "processCreateCommands": [
                "gdb-remote localhost:1234"
            ],
            "name": "lldb attach",
            "program": "${workspaceFolder}/target/riscv64gc-unknown-none-elf/release/kernel.bin",
        },
    ]
}